<%= form_for(@guideline) do |f| %>
  <% if @guideline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@guideline.errors.count, "error") %> prohibited this guideline from being saved:</h2>

      <ul>
      <% @guideline.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  <div class="field">
    <%= f.fields_for :releases do |release| %><br>
    <!-- Render the partial with the releases attributes -->
    <%= render 'release_fields', :f => release %>
    <% end %>
    <div class="links">
    	<!-- Link that shows up dinamicly the releases fields -->
	    <%= link_to_add_association 'add release', f, :releases %><br>
	</div>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
